<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".View.Activity.BarcodeScannerActivity"
    android:clipToPadding="false">

    <FrameLayout
        android:id="@+id/fl_toolbar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@color/white"
        app:layout_constraintTop_toTopOf="parent"
        android:elevation="5dp"
        android:outlineProvider="bounds"
        >

        <ImageView
            android:id="@+id/iv_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_back_green_blue"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="32dp"
            android:foreground="?attr/selectableItemBackgroundBorderless"/>

        <TextView
            android:id="@+id/tv_toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            tools:text="@string/scan_qr_code"
            android:textColor="@color/colorTextTitle"
            android:fontFamily="@font/neueeinstellungbold"
            android:textSize="17sp"
            android:gravity="center"
            />
    </FrameLayout>

    <fragment
        android:id="@+id/fragment_scan_qr_code"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/fl_toolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        class="com.moselo.HomingPigeon.View.Fragment.BarcodeScannerFragment"
        />

    <fragment
        android:id="@+id/fragment_show_qr_code"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/fl_toolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        class="com.moselo.HomingPigeon.View.Fragment.ShowQRFragment"
        />

</android.support.constraint.ConstraintLayout>